[database]
url = "sqlite:///data/corporate.db"

[auth]
# password default: admin123, maker123, checker123, approver123 (hash sha256 sederhana untuk demo)
# ganti di production dengan bcrypt
session_ttl_seconds = 3600
